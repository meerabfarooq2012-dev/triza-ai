{
  "framework": "nextjs",
  "buildCommand": "node scripts/switch-db.mjs && prisma generate && next build",
  "installCommand": "bun install",
  "regions": ["sin1"],
  "env": {
    "NEXT_PUBLIC_SOCKET_DISABLED": "true"
  }
}
