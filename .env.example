# =============================================================================
# Thiora - Environment Variables Template
# =============================================================================
# Copy this file to .env.local and fill in the values
# For Vercel deployment, add these in Project Settings > Environment Variables

# ── DATABASE (Required) ────────────────────────────────────────────────────
# Supabase PostgreSQL connection string (with pgbouncer for Vercel)
DATABASE_URL=postgresql://postgres:[YOUR-PASSWORD]@aws-0-[REGION].pooler.supabase.com:6543/postgres?pgbouncer=true
# Direct connection for Prisma migrations (without pgbouncer)
DIRECT_URL=postgresql://postgres:[YOUR-PASSWORD]@aws-0-[REGION].pooler.supabase.com:5432/postgres

# ── AUTH & SECURITY ────────────────────────────────────────────────────────
JWT_SECRET=your-super-secret-jwt-key-change-this
CSRF_SECRET=your-csrf-secret-key-change-this
ADMIN_SETUP_KEY=change-this-to-a-random-secret
ADMIN_EMAIL=admin@thiora.com
ADMIN_PASSWORD=

# ── SUPABASE STORAGE ───────────────────────────────────────────────────────
SUPABASE_SERVICE_KEY=your-supabase-service-role-key

# ── GOOGLE OAUTH (Optional) ────────────────────────────────────────────────
NEXT_PUBLIC_GOOGLE_CLIENT_ID=your-google-client-id

# ── EMAIL (Resend) ─────────────────────────────────────────────────────────
RESEND_API_KEY=re_xxxxxxxx
EMAIL_FROM_DOMAIN=your-domain.vercel.app

# ── PAYMENT GATEWAYS (Optional - sandbox defaults available) ───────────────
EASYPAISA_MERCHANT_ID=
EASYPAISA_STORE_ID=
EASYPAISA_API_KEY=
EASYPAISA_API_URL=
JAZZCASH_MERCHANT_ID=
JAZZCASH_PASSWORD=
JAZZCASH_INTEGRITY_SALT=
JAZZCASH_API_URL=
PAYMENT_GATEWAY_MODE=sandbox
PAYMENT_CALLBACK_BASE_URL=https://your-domain.vercel.app

# ── PAYFAST (International Card Payments) ──────────────────────────────────
# NOTE: Stripe does not support Pakistan — replaced with PayFast
# Get PayFast credentials from https://www.payfast.co.za
PAYFAST_MERCHANT_ID=
PAYFAST_MERCHANT_KEY=
PAYFAST_PASSPHRASE=
PAYFAST_API_URL=https://api.payfast.co.za
PAYFAST_SANDBOX_API_URL=https://sandbox.payfast.co.za

# ── CRYPTO (Direct Wallet - No KYC Required!) ──────────────────────────────
# Your escrow wallet addresses where buyers will send crypto
# Get these from your Exodus / Trust Wallet app
CRYPTO_BTC_WALLET=your-btc-wallet-address
CRYPTO_ETH_WALLET=your-eth-wallet-address
CRYPTO_SOL_WALLET=your-sol-wallet-address
CRYPTO_LTC_WALLET=
# Optional: Blockchain API keys for automatic verification (free tiers work)
BLOCKCYPHER_TOKEN=
ETHERSCAN_API_KEY=

# ── WEB PUSH NOTIFICATIONS ─────────────────────────────────────────────────
# Generate with: npx web-push generate-vapid-keys
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
VAPID_SUBJECT=https://your-domain.vercel.app

# ── AI ─────────────────────────────────────────────────────────────────────
# TRIZA principle: NO external AI APIs, NO LLM, NO API keys.
# All AI features are 100% local TypeScript. These keys are intentionally
# removed — the codebase no longer calls Cohere, Gemini, or any LLM.

# ── URLs ───────────────────────────────────────────────────────────────────
NEXT_PUBLIC_PLATFORM_URL=https://your-domain.vercel.app
NEXT_PUBLIC_APP_URL=https://your-domain.vercel.app
NEXT_PUBLIC_BASE_URL=https://your-domain.vercel.app

# ── SENTRY (Optional - Error Monitoring) ──────────────────────────────────
# If not configured, the app works normally with no errors (graceful no-op)
# Get your DSN from: https://sentry.io/settings/projects/ → Client Keys → DSN
# Client-side DSN (exposed to browser — must use NEXT_PUBLIC_ prefix)
NEXT_PUBLIC_SENTRY_DSN=
# Server-side DSN (not exposed to browser — no NEXT_PUBLIC_ prefix)
SENTRY_DSN=
# Sentry organization slug (for source maps upload during builds)
SENTRY_ORG=
# Sentry project slug (for source maps upload during builds)
SENTRY_PROJECT=
# Sentry auth token (for source maps upload during builds)
# Generate at: https://sentry.io/settings/account/api/auth-tokens/
SENTRY_AUTH_TOKEN=

# ── SOCKET (Auto-detected on Vercel - no config needed) ────────────────────
# Real-time features gracefully degrade to REST polling on Vercel
# NEXT_PUBLIC_SOCKET_DISABLED=true  # Uncomment to force-disable sockets
